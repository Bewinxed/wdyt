{
  "name": "second-opinion",
  "version": "0.1.0",
  "type": "module",
  "description": "Code review context builder for LLMs - get a second opinion on your code",
  "main": "src/cli.ts",
  "bin": {
    "second-opinion": "./src/cli.ts",
    "rp-cli": "./src/cli.ts"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build ./src/cli.ts --compile --outfile rp-cli",
    "test": "bun test"
  },
  "dependencies": {
    "citty": "^0.1.6"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0"
  }
}
